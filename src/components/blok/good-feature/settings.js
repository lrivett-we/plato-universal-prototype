const settings = {
  blokSettings: {
    display_name: "Good Feature",
    is_root: false,
    is_nestable: true,
    schema: {
      name: {
        type: "text",
      },
    },
  },
};

export default settings;
