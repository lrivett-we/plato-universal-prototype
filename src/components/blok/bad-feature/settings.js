const settings = {
  blokSettings: {
    display_name: "Bad Feature",
    is_root: false,
    is_nestable: true,
    schema: {
      name: {
        type: "text",
      },
    },
  },
};

export default settings;
