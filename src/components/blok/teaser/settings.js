const settings = {
  blokSettings: {
    display_name: "Teaser",
    is_root: false,
    is_nestable: true,
    schema: {
      headline: {
        type: "text",
        pos: 0,
      },
    },
  },
};

export default settings;
