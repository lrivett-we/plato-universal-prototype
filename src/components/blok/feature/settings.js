const settings = {
  blokSettings: {
    display_name: "Feature",
    is_root: false,
    is_nestable: true,
    schema: {
      name: {
        type: "text",
      },
    },
  },
};

export default settings;
