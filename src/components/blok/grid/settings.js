const settings = {
  blokSettings: {
    display_name: "Grid",
    is_root: false,
    is_nestable: true,
    schema: {
      columns: {
        type: "bloks",
      },
    },
  },
};

export default settings;
