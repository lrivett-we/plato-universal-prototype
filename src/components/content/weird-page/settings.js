const settings = {
  blokSettings: {
    display_name: "Weird Page",
    is_root: true,
    is_nestable: false,
    schema: {
      body: {
        type: "bloks",
      },
    },
  },
};

export default settings;
