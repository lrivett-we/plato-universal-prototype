const settings = {
  blokSettings: {
    display_name: "Page",
    is_root: true,
    is_nestable: false,
    schema: {
      body: {
        type: "bloks",
      },
    },
  },
};

export default settings;
